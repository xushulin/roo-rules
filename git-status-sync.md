## Git 变更规则

你在完成代码修改任务（包括文档同步）后，**必须自动执行 Git 变更总结**，无需请求我的批准，流程如下：

1. **检查工作区状态**：运行 `git status`，确认有哪些文件被修改。
2. **推荐git commit提交信息**：提交信息需遵循 [Conventional Commits](https://www.conventionalcommits.org/) 规范（如 `feat:`、`fix:`、`docs:` 等）需要使用简体中文。
